#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
#define sz(x) ((int)x.size())
#define ff first
#define ss second
#define all(x) begin(x), end(x)
const int MOD = 1e9 + 7;
// *-> KISS*

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int test = 153;
    //cin >> test;
    ofstream fout;
    fout.open("output.txt", ios :: trunc | ios :: out | ios :: in);
    for(int t_ = 1; t_ < test; t_++)
    {
        fout << "generator " << t_ << " > $" << endl;
    }
    return 0;
}
// -> Keep It Simple Stupid!
