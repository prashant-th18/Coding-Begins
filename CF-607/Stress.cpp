#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
#define sz(x) ((int)x.size())
#define ff first
#define ss second
#define all(x) begin(x), end(x)
// *-> KISS*
int32_t main() {
    string a = string(5000, 'a'), b = string(5000, 'b');
    fstream fout;
    fout.open("inp.out", ios :: out | ios :: trunc);
    fout << 1 << endl << a << ' ' << b;
    return 0;
}
// -> Keep It Simple Stupid!
