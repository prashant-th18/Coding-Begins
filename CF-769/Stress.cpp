#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
#define sz(x) ((int)x.size())
#define ff first
#define ss second
#define all(x) begin(x), end(x)
// *-> KISS*
int32_t main() {
    ofstream fout("inp");
    fout << 100 << endl;
    for(int i = 0; i < 100; ++i)
    {
        fout << 1 << ' ' << 1000 << endl;
    }
    return 0;
}
// -> Keep It Simple Stupid!
